# PRISMA ORM

### SYNC DB

```sh
# sync database
npx prisma db push

# reset database
npx prisma migrate reset
```

### SEED

#### seed files: /prisma/seed/

#### daftarkan pada: /prisma/seed.ts

```sh
# do seed
npx prisma db seed
```
